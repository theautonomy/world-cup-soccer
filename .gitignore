# Dependencies
node_modules/
app/node_modules/

# React build output (deployed via CI, not committed)
app/dist/

# OS
.DS_Store
Thumbs.db

# output/teams/ (legacy HTML) — still tracked for reference
# data/teams/*.json is intentionally tracked — JSON is the source of truth committed to repo

